name: flutter_api_calling
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  bloc: ^8.1.1
  flutter:
    sdk: flutter
  http: ^0.13.6
  scrollable_positioned_list: ^0.3.8

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
